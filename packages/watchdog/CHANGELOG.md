# Changelog

## 0.1.0

- First release: `armWatchdog`, a worker-thread watchdog over a SharedArrayBuffer stamp, with log (repeat, count, recovery) and kill actions, and `renderWatchdogLine` for lines built only from shared memory.
